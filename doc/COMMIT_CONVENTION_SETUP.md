# コミット規約セットアップガイド

Conventional Commits形式に従ったコミット規約を設定する手順です。

## 📦 必要なパッケージのインストール

プロジェクトのルートディレクトリで以下を実行：

```bash
# commitlintとその設定をインストール
npm install -D @commitlint/cli @commitlint/config-conventional

# Huskyをインストール（Git hooks用）
npm install -D husky

# Huskyを初期化
npx husky install

# commit-msgフックを追加（既に作成済み）
# .husky/commit-msg ファイルが存在することを確認

# pre-commitフックを追加（既に作成済み）
# .husky/pre-commit ファイルが存在することを確認
```

## 🔧 package.jsonの設定

`package.json`に以下を追加：

```json
{
  "scripts": {
    "prepare": "husky install",
    "lint": "next lint",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}
```

## ✅ 動作確認

### コミットメッセージの検証テスト

```bash
# ✅ 正しい形式のコミット（成功するはず）
git commit -m "feat: add user authentication"

# ❌ 間違った形式のコミット（エラーになるはず）
git commit -m "add user authentication"
```

### pre-commitフックのテスト

```bash
# 変更をステージング
git add .

# コミットを試みる（LintとFormatチェックが実行される）
git commit -m "feat: test commit"
```

## 📝 コミットメッセージの形式

### 基本形式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type（必須）

- `feat`: 新機能
- `fix`: バグ修正
- `docs`: ドキュメントのみの変更
- `style`: コードの動作に影響しない変更（フォーマット、セミコロンなど）
- `refactor`: バグ修正や機能追加を伴わないコード変更
- `perf`: パフォーマンス改善
- `test`: テストの追加・変更
- `build`: ビルドシステムや依存関係の変更
- `ci`: CI設定の変更
- `chore`: その他の変更（上記に当てはまらないもの）
- `revert`: 以前のコミットの取り消し

### Scope（オプション）

変更が影響する範囲を指定：

- `auth`: 認証関連
- `ui`: UIコンポーネント
- `api`: API関連
- `db`: データベース関連
- `config`: 設定ファイル

### Subject（必須）

- 50文字以内
- 現在形、命令形で記述
- 最初の文字は大文字にしない
- 末尾にピリオドを付けない

### Body（オプション）

- 変更の理由や背景を説明
- 72文字で折り返す
- 現在形で記述

### Footer（オプション）

- 破壊的変更（BREAKING CHANGE）の説明
- 関連するIssue番号

## 📚 コミット例

### シンプルな例

```bash
git commit -m "feat: add search functionality"
git commit -m "fix: resolve login error"
git commit -m "docs: update README"
```

### Scope付きの例

```bash
git commit -m "feat(auth): add OAuth login"
git commit -m "fix(ui): correct button alignment"
git commit -m "refactor(api): simplify error handling"
```

### Body付きの例

```bash
git commit -m "feat: add dark mode

ユーザーがダークモードとライトモードを切り替えられるようにしました。
設定はLocalStorageに保存され、次回訪問時も維持されます。"
```

### 破壊的変更の例

```bash
git commit -m "feat(api): change authentication endpoint

BREAKING CHANGE: /api/auth/login エンドポイントが /api/v2/auth/login に変更されました。
既存のクライアントコードの更新が必要です。"
```

## 🛠️ トラブルシューティング

### Huskyが動作しない場合

```bash
# Huskyを再インストール
rm -rf .husky
npm install -D husky
npx husky install

# フックファイルの実行権限を確認
chmod +x .husky/commit-msg
chmod +x .husky/pre-commit
```

### commitlintが動作しない場合

```bash
# commitlintの設定を確認
cat .commitlintrc.json

# 手動でテスト
echo "feat: test" | npx commitlint
```

### pre-commitフックが失敗する場合

```bash
# Lintエラーを確認
npm run lint

# Formatエラーを確認
npm run format:check

# 自動修正を試す
npm run format
```

## 📋 チェックリスト

- [ ] commitlintとHuskyをインストール
- [ ] `.commitlintrc.json`が存在することを確認
- [ ] `.husky/commit-msg`が存在することを確認
- [ ] `.husky/pre-commit`が存在することを確認
- [ ] `package.json`に`prepare`スクリプトを追加
- [ ] コミットメッセージの検証が動作することを確認
- [ ] pre-commitフックが動作することを確認

---

**設定完了後、統一されたコミットメッセージで開発を進められます！📝**

